{% extends "EkynaAdminBundle:Entity:sided.html.twig" %}

{% set resource = _context[resource_name] %}

{% block title %}
<h1>{{ (resource_id ~ '.header.remove')|trans({'%name%': resource})|raw }}</h1>
{% endblock title %}

{% block actions %}
{{ admin_resource_btn(resource, 'list') }}
{{ admin_resource_btn(resource, 'new') }}
{% endblock actions %}

{% block main %}
<form action="{{ path(route_prefix ~ '_remove', {(resource_name ~ 'Id'): resource.id}) }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
    <div class="form-body">
	    {{ bootstrap_set_style('horizontal') }}
        {{ bootstrap_set_col_size('md') }}
		{{ form_widget(form) }}
    </div>
    {{ ui_form_footer(form) }}
</form>
{% endblock main %}